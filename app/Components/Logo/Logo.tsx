import Link from 'next/link';

const Logo = () => {
    return (
        <Link href={'/'}>
            <img src="/allFolders/Image/Logo.svg" alt="Logo" />
        </Link>
    );
};

export default Logo;
