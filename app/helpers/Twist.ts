// export const Twistt = (
// direction: 'forward' | 'backward',
// setCurrentTime: (time: number) => void,
// ) => {
// const increment = direction === 'forward' ? 5 : -5;

// let twist = .current.currentTime + increment;
// twist = Math.max(0, Math.min(twist, TabletaudioRef.current.duration));
// TabletaudioRef.current.currentTime = twist;
// setCurrentTime(twist);
// }
// ;

// export const playNext = (currentId: number) => {
//     // Logic to determine the next track ID
//     const totalTracks = 10; // Example value
//     const nextId = (currentId + 1) % totalTracks; // Loop back to start if needed
//     return { newIndex: nextId };
// };
